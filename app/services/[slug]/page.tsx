import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link"; import EstimateForm from "../../components/EstimateForm"; import { business, siteUrl } from "../../data/site";
export function generateStaticParams(){return business.services.map(s=>({slug:s.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params; const s=business.services.find(x=>x.slug===slug); if(!s)return {}; return {title:`${s.name} in Columbus, GA`,description:s.meta,alternates:{canonical:`/services/${s.slug}`},openGraph:{title:`${s.name} in Columbus, GA`,description:s.meta,url:`${siteUrl}/services/${s.slug}`,images:[s.image]}};}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const s=business.services.find(x=>x.slug===slug)!; return <main id="main"><section className="service-hero section-shell"><div><p className="eyebrow"><span/>Concrete service</p><h1>{s.name} in Columbus, GA</h1><p>{s.meta}</p><a className="button button-primary" href="#estimate">Request an Estimate</a></div><Image src={s.image} alt={`${s.name} general concrete imagery`} width={980} height={680} priority sizes="(max-width: 900px) 100vw, 50vw"/></section><section className="section-shell split"><div><h2>Useful, property-specific planning</h2><p>{s.description}</p><h3>Typical use cases</h3><ul>{s.uses.map(u=><li key={u}>{u}</li>)}</ul></div><div className="detail-card"><h2>What to share</h2><p>Include the project city or ZIP code, approximate size if known, timeline and any access or photo details that would help the estimate conversation.</p><Link href="/#services">View all services</Link></div></section><EstimateForm /></main>}
